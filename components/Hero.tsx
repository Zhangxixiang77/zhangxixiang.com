/**
 * Hero — the name, set big.
 *
 * The tagline moves into the Q & A card (one of those questions
 * answers itself with "他强任他强, i am zhang·xi·xiang.").
 */
export default function Hero() {
  return (
    <header className="pt-20 pb-6 text-center md:pt-24">
      <h1 className="animate-fadeup font-serif text-[36px] font-normal tracking-[0.04em] text-ink md:text-[56px]">
        Zhang Xixiang
      </h1>
    </header>
  );
}
